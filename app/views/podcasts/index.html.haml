%h1 Listing podcasts

= form_tag podcasts_path, method: "get" do
  = label_tag :query, "Search podcasts: "
  = search_field_tag :query
  = submit_tag "Search"

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @podcasts.each do |podcast|
      %tr
        %td= link_to podcast.title, podcast
        %td= link_to 'Edit', edit_podcast_path(podcast)
        %td= link_to 'Destroy', podcast, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Podcast', new_podcast_path
